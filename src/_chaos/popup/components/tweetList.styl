:local(.wrapper)
	margin-bottom 1px

:local(.tweet)
	border-top 1px solid rgba(192, 192, 192, .5)

	// just to avoid scrollbar intersection in OS X
	.osx &
		padding-right: 12px;

	&:first-child
		border-top initial
